<div class="container index">
  <% const defaultLanguage = oranges_default_language(); %>
  <div class="post-list">
    <!-- 遍历渲染文章 -->
    <% page.posts.forEach(function(item) { %>
      <div class="post-item" title="<%- item.title %>">
        <div class="time-m-d"><%- item.date.format("YYYY-MM-DD") %></div>
        <div class="title">
          <a href="<%- url_for(item.path) %>">
            <% if(item.title !== "") { %>
              <% if (item.top) { %>
                <span><span title="<%= oranges_translate('pinned', defaultLanguage) %>" data-i18n-key="pinned" data-i18n-attr="title" class="icon iconfont icon-pin-top post-top"></span><%- item.title %></span>
              <% } else { %>
                <span><%- item.title %></span>
              <% } %>
            <% } else { %>
              <span data-i18n-key="untitled"><%= oranges_translate('untitled', defaultLanguage) %></span>
            <% } %>
          </a>
        </div>
      </div>
    <% }) %>
  </div>
  <nav class="post-navigation">
    <% if(page.prev !== 0){ %>
      <a href="<%- url_for(page.prev_link)%>"><i class="iconfont icon-angleleft"></i></a>
    <% } %>
    <span class="page-num"><%- page.current %> / <%- page.total -%></span>
    <% if(page.next !== 0){ %>
      <a href="<%- url_for(page.next_link)%>"><i class="iconfont icon-angleright"></i></a>
    <% } %>
  </nav>
</div>
